# Copy to .env and fill in
OPENAI_API_KEY=sk-...

# Models
OPENAI_CHAT_MODEL=gpt-4o
OPENAI_CHAT_REASONING_EFFORT=low         # gpt-5/o-series only: none|low|medium|high|xhigh (lower = faster first token)
OPENAI_ROUTER_MODEL=gpt-4o-mini          # cheap model for query rewrite + rerank
OPENAI_EMBED_MODEL=text-embedding-3-small

# RAG
RAG_TOP_K=3
RAG_MIN_SCORE=0.30
RERANK_CANDIDATES=8                       # Qdrant hits fetched before reranking

# Latency toggles — each is 1 extra LLM call/turn. false = faster; enable if quality drops.
RAG_RERANK_ENABLED=false                  # LLM reorders KB hits (else vector score order)
AGENT_THINK_ENABLED=false                 # assessment call before answer (delays first token)
AGENT_EVAL_END_ENABLED=false              # end-of-chat check (else trust main LLM conv_status)

# Qdrant (vector store) — populate with: python scripts/embed_to_qdrant.py
QDRANT_URL=http://localhost:6333
QDRANT_API_KEY=
QDRANT_COLLECTION=chatbot_sd_kb

# History — chat history is kept client-side (localStorage); this only caps
# how many prior turns the client sends to the LLM for context.
HISTORY_TURNS=6

# Kafka — set KAFKA_ENABLED=true to consume from ai-agent-chat topic
KAFKA_ENABLED=false
KAFKA_HOST=localhost:9092
KAFKA_INPUT_TOPIC=ai-agent-chat
KAFKA_OUTPUT_TOPIC=bot-agent-service
KAFKA_GROUP_ID=ai-agent-consumer

# WebSocket (SocketCluster) — set WS_ENABLED=true to stream tokens to frontend
WS_ENABLED=false
WS_URL=ws://localhost:8001/socketcluster/

# MinIO — image storage
MINIO_ENDPOINT=10.91.120.104:30614       # S3 API port
MINIO_ACCESS_KEY=npwxCgwLVd8pObDS
MINIO_SECRET_KEY=ocsfWEAYY0AfkSjYRTIml8rfr9vthAWA
MINIO_SECURE=false
MINIO_PUBLIC_BASE_URL=http://10.91.120.104:30614   # public base URL for ZALO
MINIO_BUCKET_NAME=chatbotmafc

# Langfuse — set LANGFUSE_ENABLED=true to trace all LLM calls
LANGFUSE_ENABLED=false
LANGFUSE_SECRET_KEY=sk-lf-...
LANGFUSE_PUBLIC_KEY=pk-lf-...
LANGFUSE_BASE_URL=https://cloud.langfuse.com
LANGFUSE_TIMEOUT=5000